# Created using @tscircuit/plop (npm install -g @tscircuit/plop)
name: Bun Test

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Run tests
        run: bun test --timeout 15000 || true  # prevent job failure before uploading artifacts

      - name: Upload diff artifacts
        uses: actions/upload-artifact@v4
        with:
          name: diff-images
          path: |
            **/*.diff.png
          if-no-files-found: ignore
